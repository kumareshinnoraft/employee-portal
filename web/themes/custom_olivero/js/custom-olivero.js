/**
 * @file
 * custom_olivero behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.customOlivero = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
